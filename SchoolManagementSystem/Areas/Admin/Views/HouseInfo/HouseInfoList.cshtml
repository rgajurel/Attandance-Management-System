
<div class="row" style="margin-top:10px">
    <div class="col-lg-12">
        <div class="pageHeaderBox11">


        </div>
    </div>
    <!-- /.col-lg-12 -->
</div>



@using Kendo.Mvc.UI
@(Html.Kendo().Grid<DomainEntities.HouseInfo>()
    .Name("HouseInfoGrid")
            .Columns(columns =>
            {
               
                columns.Bound(c => c.ID).Title("S.N").Width(20);
                columns.Bound(c => c.HouseName).Title("House Name").Width(150);


                //columns.Bound(c => c.Type).Title("Modified On").Width(50).ClientTemplate("#= customDate(data) #").Format(ViewBag.settingDateFormat);

                columns.Command(command =>
                {
                    command.Custom("Edit").Text(" ").Click("Edit");
                    command.Custom("Delete").Text(" ").Click("Delete");

                }).Width(50).Title("Action");

            })


                     .ToolBar(toolbar =>
                     {
                     toolbar.Template(@<text>


        <div class="right-inner-addon" style="width:200px">

            <input type="search"
                   class="form-control"
                   placeholder="Search By Name" id="FieldFilter" />

        </div>
                    </text>);
                     })


                    //.Filterable()
                    //.Sortable(sortable => sortable
                    // .AllowUnsort(true)
                    // .SortMode(GridSortMode.MultipleColumn))
                    .Pageable(
              page => page.Refresh(true)
             .PageSizes(new int[] { 10, 20, 40, 60, 80, 100 })
              .PreviousNext(true))

            .Reorderable(reorder => reorder.Columns(true))
                    .DataSource(dataSource => dataSource
                        .Ajax()
                        .ServerOperation(false)
                                .Model(model =>
                                {
                                    model.Id(p => p.ID);
                                    model.Field(p => p.HouseName).Editable(false);

                                })
                  .Read(read => read.Action("GetAll", "HouseInfo"))// Action invoked when the user saves a new data item
                  .PageSize(5)
             )
.Events(ev => ev.DataBound("resetRowNumber"))

)
</div>







